Clean up bst unit tests

Add test to generate stream of random values for bst
Make it seeded to replicate failures


// Polymorphic allocator
implement extend and extend_bytes

use a consistent naming scheme for classes


split the four sorting inputs into different functions